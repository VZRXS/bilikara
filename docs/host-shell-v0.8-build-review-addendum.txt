Bilikara v0.8 packaged-build review addendum
=============================================

Status: authoritative correction to the historical shell freeze
Supersedes: conflicting navigation, Stage, width, toolbar, and chrome decisions

Corrected contract
------------------

* Queue and History are direct destinations. Product order is Queue, History,
  Request, Gatcha, Session Users.
* Next is Stage-owned Next: the one command button lives in the Now Playing
  header and nowhere in Queue or another tool.
* The shell is Stage plus a stable tool card plus an independent fixed
  right-side tool rail. The rail is a separate persistent surface with its own
  border and gap; it is never fused to either card, including narrow mode.
* There is one stable tool width in each responsive state. Tool and Request
  subview selection never changes width or dock/overlay policy.
* Full, compact and narrow are width-and-height measured Stage modes. One
  ResizeObserver measures the actual Stage container. Every mode keeps the one
  existing extended-control subtree collapsed behind a 52px circular button at
  the Stage's lower-left corner. Its bounded floating panel prefers an
  upper-right expansion, clamps to the available viewport, and closes from its
  backdrop, close button or Escape; no media or controls are cloned or
  reparented.
* The top surface is a one-line icon-plus-label global toolbar. Constrained
  widths use accessible icon-only actions. Update availability is a theme-color
  ring around the existing Service health check, not a second marker slot.
* Platform-specific integrated window chrome uses a frameless toolbar drag
  region and native window commands on Windows; macOS retains traffic lights,
  native behavior and an overlay titlebar with content inset. Browser and Linux
  show no fake controls or dead titlebar gap.
* Product labels are Request / 点歌 / リクエスト and Gatcha. Discover remains
  an internal Request subview; internal `random` identifiers may remain.

Composition and order
---------------------

  integrated global toolbar
  +-----------------------------------------------------------------------+
  | persistent Stage | stable active tool card | gap | fixed right rail |
  +-----------------------------------------------------------------------+

DOM/Tab order is toolbar, Stage, active tool content, then the right rail. The
rail is a vertical roving tab stop: Up/Down/Home/End move focus and Enter/Space
activates. Pointer, keyboard and touch use the same five destinations.

Scroll-owner table
------------------

Exactly one vertical owner is active for each row. Ancestors from shell to the
named owner use `min-height: 0` and hidden overflow; growing grids below an
owner use visible vertical overflow.

Surface / level                         Vertical scroll owner
--------------------------------------  ------------------------------------
Queue upcoming rows                     #playlist
History rows                            #history-list
Search / Shared                         #lark-search-results
Search / Local                          #search-results
Discover / Categories home              #request-discover-categories
Discover / Categories detail/results    #request-discover-categories
Discover / Name alphabet/tags/items     #request-discover-name
Discover / Artist alphabet/tags/items   #request-discover-artist
Sources / Added UIDs owner/items        #request-sources-uids-scroll
Sources / Favorites folders/items       #request-sources-favorites-scroll
Gatcha                                  #gatcha-stage
Session Users                           #session-user-list
Service / Appearance popover            active .cache-panel
Song detail                             .song-detail-body
Binding / favorites task sheet          active .selection-option-list
Pool configuration task sheet           #gatcha-pool-source-list
Developer task-sheet options            .developer-tag-reset-fields
Floating Stage control panel            #stage-control-tray

Horizontal tabs and category strips may scroll inline but never own the
vertical wheel. Owners contain overscroll so body and Stage never become
fallbacks. Queue, History, Request modes and hierarchical parent levels keep
independent scroll state; Back restores the exact recorded parent level.

Stage and ownership
-------------------

Full mode requires both enough measured inline space and enough measured block
space for the real header and a useful fitted player row. Compact is selected
when either dimension cannot fit. Narrow always uses a bounded compact Stage
band while the tool card receives the remaining height; the independent rail
continues to span the shell body at the far right.

The floating panel contains the existing audio variants, AV delay, volume and
Key controls. Its transparent surrounding backdrop and close button close it;
Escape closes it and restores its opener. The one
`state.hostPlaybackSession`, `#player-frame`, video and audio retain identity;
tray and pure navigation changes issue zero player commands.

Persistent Stage, tool and rail surfaces use borders and tonal separation, not
large clipped shadows or persistent backdrop filters. Strong shadows are for
floating layers. Empty, status, loading, error and inline-help messages share
bounded padding, wrapping, readable line-height and honest height.
